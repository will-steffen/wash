{
  "name": "wash-client-spec",
  "version": "1.0.0",
  "description": "node client to consume and test wash",
  "scripts": {
    "test": "npm run set && npm run exec",
    "set": "ncp ../../public_html/wash/ ../wash/ -f",
    "exec": "./node_modules/.bin/jasmine-node spec"
  },
  "author": "will-steffen",
  "license": "MIT",
  "dependencies": {
    "jasmine-node": "^1.14.5",
    "ncp": "^2.0.0",
    "request": "^2.87.0"
  }
}
